{
  "name": "presence-app-website",
  "version": "0.1.0",
  "description": "Marketing and information website for the Practicing Presence app (presence.app)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.2",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/open-sans": "^5.2.5",
    "@fontsource/quicksand": "^5.2.5",
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
